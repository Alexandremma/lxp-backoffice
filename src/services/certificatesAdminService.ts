/**
 * Barrel de compatibilidade — implementação em `services/certificates/*` (C3b).
 * Preferir imports de `@/services/certificates` em código novo.
 */
export * from "@/services/certificates/index"
